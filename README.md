# Document-Parsing-Ai-agent-

## 🎯 Project Overview

This AI agent analyzes legislative PDF documents and performs:

- **Text Extraction** — Clean and structured PDF text  
- **Summarization** — Concise summary generated by Gemini  
- **Section Extraction** — Definitions, obligations, eligibility, etc.  
- **Rule Validation** — Automated compliance checks  
- **Streamlit UI** — Simple interface for uploading & analyzing documents  

---

## 🚀 Quick Start

### **Prerequisites**
- Python 3.10+
- Google Gemini API key

### **Installation**

```bash
git clone <your-repo-url>
cd ai-document-parser

pip install -r requirements.txt
```

---

## 🔑 API Key Setup (Updated)

Your API key is **hardcoded inside the app**, not entered through the UI.

Open `app.py` and update this line:

```python
GEMINI_API_KEY = "YOUR_API_KEY_HERE"
```

---

## ▶️ Run the Streamlit App

```bash
streamlit run app.py
```

Visit: **http://localhost:8501**

---

## 🖥 Run via CLI

```bash
python agent.py /path/to/document.pdf
```

Output saved as `output.json`

---

## 📁 Project Structure

```
.
├── agent.py
├── app.py
├── requirements.txt
├── output.json
├── README.md
└── example.pdf
```

---

## 🧠 Architecture

```
PDF Input
   ↓
🧹 Text Extraction (PyPDF)
   ↓
📝 Summarization (Gemini)
   ↓
🔍 Section Extraction (Gemini)
   ↓
✔️ Rule Validation (Gemini)
   ↓
📦 JSON Output
```

---

## 📊 Example Output

### **Section Extraction**
```json
{
  "definitions": "...",
  "obligations": "...",
  "responsibilities": "...",
  "eligibility": "...",
  "payments": "...",
  "penalties": "...",
  "record_keeping": "..."
}
```

### **Rule Checks**
```json
[
  {
    "rule": "Act must define key terms",
    "status": "pass",
    "evidence": "Section 2 defines...",
    "confidence": 98
  }
]
```

---

## 🧪 Testing

```bash
python agent.py /mnt/data/ukpga_20250022_en.pdf
```

---

## 📝 Design Decisions

### **Why Hardcode API Key?**
- Faster testing  
- Simpler UI  
- No repeated input  

(Do **NOT** commit your real key to GitHub.)

### **Why Gemini 2.5 Flash?**
- Fast  
- Accurate  
- Great context handling  
- Strong JSON output  

---

## 🐛 Troubleshooting

### ❌ "charmap codec" error
Fix: use UTF-8 or binary file reads (included in project)

### ❌ 404 Model Not Found
Fix: using correct model: `gemini-2.5-flash`

### ❌ Rule Count Mismatch  
Fix: One rule **correctly fails** (“enforcement or penalties”).
